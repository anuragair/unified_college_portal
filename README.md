# College ERP

Frontend scaffolding aligned to the requested structure. All HTML pages live in `Frontend/src/pages`, static assets under `Frontend/src/assets`, and shared helpers in `Frontend/src/utils`. Backend scaffolding is reserved for future API work.
